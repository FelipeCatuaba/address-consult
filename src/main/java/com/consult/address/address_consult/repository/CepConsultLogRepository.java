package com.consult.address.address_consult.repository;

import com.consult.address.address_consult.domain.CepConsultLog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CepConsultLogRepository extends JpaRepository<CepConsultLog, Long> {
}
